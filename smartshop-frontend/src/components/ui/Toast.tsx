/**
 * Toast.tsx — Re-export pattern; app uses react-hot-toast <Toaster /> in main.tsx
 *
 * PURPOSE:
 * Documents that global toasts are configured at the root, not per-page.
 */

export { Toaster } from 'react-hot-toast';
